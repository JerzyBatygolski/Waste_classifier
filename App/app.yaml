runtime: python311

instance_class: F2

automatic_scaling:
  min_instances: 0
  max_instances: 3
  target_cpu_utilization: 0.65

entrypoint: streamlit run app.py --server.port $PORT --server.address 0.0.0.0 --server.headless true

env_variables:
  API_URL: "https://waste-classifier-api-spsy2vhjeq-lm.a.run.app"
